import AuthForm from '../../components/AuthForm';
import {Container} from './styles';

function Auth() {
    return (
        <Container>
            <AuthForm/>
        </Container>
    );
}

export default Auth;
